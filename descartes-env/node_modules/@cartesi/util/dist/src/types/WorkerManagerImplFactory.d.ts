import { Signer } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import { ContractFactory, Overrides } from "@ethersproject/contracts";
import type { WorkerManagerImpl } from "./WorkerManagerImpl";
export declare class WorkerManagerImplFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides): Promise<WorkerManagerImpl>;
    getDeployTransaction(overrides?: Overrides): TransactionRequest;
    attach(address: string): WorkerManagerImpl;
    connect(signer: Signer): WorkerManagerImplFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): WorkerManagerImpl;
}
