import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { ShadowAddresses } from "../ShadowAddresses";
export declare class ShadowAddresses__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<ShadowAddresses>;
    getDeployTransaction(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): ShadowAddresses;
    connect(signer: Signer): ShadowAddresses__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): ShadowAddresses;
}
