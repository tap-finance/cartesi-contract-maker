import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { RiscVConstants } from "../RiscVConstants";
export declare class RiscVConstants__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<RiscVConstants>;
    getDeployTransaction(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): RiscVConstants;
    connect(signer: Signer): RiscVConstants__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): RiscVConstants;
}
